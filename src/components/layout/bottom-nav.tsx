"use client";
import Link from "next/link"; import { usePathname } from "next/navigation";
const items = [{ href: "/", label: "ホーム", icon: "⌂" }, { href: "/search", label: "検索", icon: "⌕" }, { href: "/create", label: "投稿", icon: "＋" }, { href: "/notifications", label: "通知", icon: "♢" }, { href: "/profile", label: "プロフィール", icon: "◉" }];
export function BottomNav() { const pathname = usePathname(); return <nav aria-label="メインナビゲーション" className="fixed inset-x-0 bottom-0 z-20 border-t border-slate-200 bg-white/95 pb-[max(0.45rem,env(safe-area-inset-bottom))] pt-1 backdrop-blur"><div className="mx-auto flex max-w-lg items-end justify-around px-2">{items.map((item) => { const active = item.href === "/" ? pathname === "/" : pathname.startsWith(item.href); return <Link key={item.href} href={item.href} className={`flex min-w-13 flex-col items-center gap-0.5 rounded-xl px-3 py-1 text-[10px] font-medium ${active ? "text-sky-600" : "text-slate-400"}`}><span className={`flex h-7 w-7 items-center justify-center text-2xl leading-none ${item.href === "/create" ? "rounded-full bg-sky-600 text-white shadow-sm" : ""}`}>{item.icon}</span>{item.label}</Link>; })}</div></nav>; }
