"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { skillCatalog } from "@/data/mock-data";
import { useApp } from "@/components/providers/app-provider";
export default function CreatePage() { const router = useRouter(); const { addPost } = useApp(); const [skillId, setSkillId] = useState(skillCatalog[4].id); const [body, setBody] = useState(""); const [videoLabel, setVideoLabel] = useState(""); const submit = (event: React.FormEvent) => { event.preventDefault(); addPost(skillCatalog.find((skill) => skill.id === skillId)!, body, videoLabel || undefined); router.push("/"); }; return <><header className="flex h-15 items-center justify-between border-b border-slate-100 px-4"><h1 className="text-xl font-bold">練習を投稿</h1><button form="post-form" className="text-sm font-bold text-sky-600">シェア</button></header><form id="post-form" onSubmit={submit} className="space-y-6 p-4"><label className="block"><span className="mb-2 block text-sm font-bold">練習している技</span><select value={skillId} onChange={(event) => setSkillId(event.target.value)} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm outline-none focus:border-sky-500">{skillCatalog.map((skill) => <option key={skill.id} value={skill.id}>{skill.apparatus} · {skill.name}</option>)}</select></label><label className="block"><span className="mb-2 block text-sm font-bold">動画 <span className="font-normal text-slate-400">（任意）</span></span><span className="flex min-h-36 flex-col items-center justify-center rounded-2xl border-2 border-dashed border-slate-200 bg-slate-50 p-4 text-center"><span className="text-2xl">▸</span><span className="mt-2 text-sm font-medium text-slate-600">動画を選択</span><span className="mt-1 text-xs text-slate-400">Version 1では動画名を記録します</span><input type="file" accept="video/*" className="mt-3 block w-full text-xs text-slate-500" onChange={(event) => setVideoLabel(event.target.files?.[0]?.name ?? "")} /></span></label><label className="block"><span className="mb-2 block text-sm font-bold">コメント</span><textarea value={body} onChange={(event) => setBody(event.target.value)} maxLength={280} placeholder="今日の練習で意識したことや、目標を書いてみよう" className="h-32 w-full resize-none rounded-xl bg-slate-100 p-3 text-sm outline-none ring-sky-200 focus:ring-2" /><span className="mt-1 block text-right text-xs text-slate-400">{body.length}/280</span></label><button className="w-full rounded-xl bg-sky-600 py-3 font-bold text-white shadow-sm">練習をシェアする</button></form></>; }
