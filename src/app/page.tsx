"use client";
import { PostCard } from "@/components/posts/post-card"; import { useApp } from "@/components/providers/app-provider";
export default function Home() { const { posts } = useApp(); return <><header className="sticky top-0 z-10 flex h-15 items-center justify-between border-b border-slate-100 bg-white/95 px-4 backdrop-blur"><div><p className="text-lg font-black tracking-tight text-slate-900">Gym<span className="text-sky-600">Skill</span></p><p className="text-[11px] text-slate-400">今日の練習を、次の力に。</p></div><button aria-label="表示設定" className="rounded-full bg-slate-100 p-2 text-slate-500">☰</button></header><section><div className="px-4 pb-2 pt-5"><h1 className="text-xl font-bold text-slate-900">みんなの練習</h1><p className="mt-1 text-sm text-slate-500">一歩ずつの積み重ねを応援しよう。</p></div>{posts.map((post) => <PostCard key={post.id} post={post} />)}</section></>; }
